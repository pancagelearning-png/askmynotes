services:
  - type: web
    name: askmynotes
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PORT
        value: 7860
